<?xml version="1.0" encoding="UTF-8"?>

<xwikidoc>
  <web>PresenceCode</web>
  <name>Store</name>
  <language/>
  <defaultLanguage/>
  <translation>0</translation>
  <parent>PresenceCode.WebHome</parent>
  <creator>xwiki:XWiki.Admin</creator>
  <author>xwiki:XWiki.Admin</author>
  <customClass/>
  <contentAuthor>xwiki:XWiki.Admin</contentAuthor>
  <creationDate>1365943180000</creationDate>
  <date>1365945426000</date>
  <contentUpdateDate>1365945426000</contentUpdateDate>
  <version>1.1</version>
  <title/>
  <template/>
  <defaultTemplate/>
  <validationScript/>
  <comment/>
  <minorEdit>false</minorEdit>
  <syntaxId>xwiki/2.1</syntaxId>
  <hidden>false</hidden>
  <content>{{groovy}}
import groovy.sql.Sql

if (xcontext.action == 'get') {
  if (request.datetime) {
    Date datetime = new Date(Long.valueof(request.datetime));

    def logs = new XmlParser().parse(request.inputStream);

    if (logs.elements.size() &gt; 0) {
      sql = Sql.newInstance('jdbc:mysql://localhost/xwiki', 'xwiki', 'xwiki', 'com.mysql.jdbc.Driver');
      for (element in logs.elements) {
        def mac = Integer.valueOf(element.replaceAll("[:-\\.]", ""), 16)
        sql.execute('insert into adm_presence_stat (mac, datetime) values (?, ?)', [mac, datetime])
      }
      sql.close();
    }

    print 'OK';
  } else {
    print 'ERROR: "datetime" parameter is required'
  }
} else {
  print '{{info}}This page is used as an entry point to save Presence application data and is not meant to be used as wiki page.{{/info}}';
}
{{/groovy}}</content>
</xwikidoc>
